spring.application.name=hrm-pro

# MySQL Database Connection
spring.datasource.url=jdbc:mysql://localhost:3306/hrm_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=dinhne123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always
spring.sql.init.continue-on-error=true
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT Secret Properties
app.jwtSecret=hrm_super_secret_key_which_must_be_long_enough_for_hmac_sha256_1234567890
app.jwtExpirationMs=86400000

# Open Port (optional)
server.port=8080
